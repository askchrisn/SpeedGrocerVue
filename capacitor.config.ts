import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.speedgrocervue',
  appName: 'speedgrocervue',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
